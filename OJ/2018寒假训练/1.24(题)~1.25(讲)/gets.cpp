#include<stdio.h>
#include<stack>
#include<string.h>
using namespace std;
char v[100000000];

int main ()
{
	stack<int>s;
	int T;

	scanf("%d",&T);
	while(T--)
	{
		gets(v);
		int len=strlen(v);
		for(int i=0; i<len; i++)
		{
			
		}
		printf("\n");


	}
	return 0;
}
