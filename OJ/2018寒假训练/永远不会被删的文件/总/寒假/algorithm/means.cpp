#include<stdio.h>
#include<math.h>//double sqrt(double)
#include<algorithm>
#include<string.h>//strlen(),memset(),strcmp();

#include<iostream>
using namespace std;

typedef long long ll;
const int MM=1e9+7;
const int MX=1e3+10;
int cnt计数，ans答案，flag记号，
数组名，check[] ,prime[]， 
unt 结构体， 
初始化
a[MX]={0};
memset(a,0,sizeof(a));
int main(void)
{
	
	
	
	
	retrun 0;
}
while(scanf("%d",&t))
	{
		if(t==0) break;
	}
while(scanf("%d",&t)&&t)	
